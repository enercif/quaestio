# Drizzle
DATABASE_URL="postgres://root:mysecretpassword@localhost:5432/local"
PUBLIC_BASE_URL="http://localhost:5173"
ORIGIN="http://localhost:5173"

# Better Auth
# For production use 32 characters and generated with high entropy
# https://www.better-auth.com/docs/installation
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="" # Base URL of your app

# SMTP (invite emails)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Optional: auto-create the first admin account at server startup instead of
# showing the setup screen. Only used while no user exists yet.
ADMIN_NAME=""
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
